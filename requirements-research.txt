# Research & data-sourcing dependencies
# ======================================
# These packages are NOT part of the production runtime (requirements.txt).
# They are used for:
#   - Offline data collection and exploratory analysis
#   - SPADL event conversion (socceraction)
#   - Live schedule / standings scraping (soccerdata / FBref)
#   - StatsBomb Open Data ingestion (statsbombpy)
#
# IMPORTANT: socceraction 1.x requires numpy<2.0 and pandas<3.0, which
# conflicts with the production pinned versions.  Install this file in a
# separate virtual environment or Colab notebook — NEVER alongside
# requirements.txt.
#
# Usage:
#   python -m venv .venv-research
#   .venv-research\Scripts\activate
#   pip install -r requirements-research.txt

# StatsBomb Open Data
statsbombpy>=1.4,<2

# SPADL event model (requires numpy<2, pandas<3)
socceraction>=1.5,<2

# Live schedule / standings scraping (FBref, Sofascore, ESPN…)
soccerdata>=1.7,<2

# Data tools commonly used in analysis notebooks
jupyter>=1.1,<2
matplotlib>=3.8,<4
seaborn>=0.13,<1

# Compatible numpy/pandas for this environment
# (pin to avoid conflict with socceraction)
numpy>=1.26,<2.0
pandas>=2.1,<3.0
scipy>=1.11,<2.0
pyarrow>=18,<24
lxml>=4.9.3,<5.0.0
