name: Weekly model retraining

on:
  schedule:
    # Monday 05:00 Turkey time.
    - cron: "0 2 * * 1"
  workflow_dispatch:
    inputs:
      promote_candidate:
        description: Publish this validated candidate as the live model
        required: true
        default: false
        type: boolean
      candidate_version:
        description: Shadow candidate version to promote after it passes the live gate
        required: false
        type: string

concurrency:
  group: weekly-model-retraining
  cancel-in-progress: false

permissions:
  contents: write

env:
  API_FOOTBALL_KEY: ${{ secrets.API_FOOTBALL_KEY }}
  SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
  SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

jobs:
  retrain:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: python -m pip install --disable-pip-version-check -r requirements-dev.txt
      - name: Run unit tests
        run: python -m pytest -q
      - name: Train and validate models
        run: python -m models.train_model
      - name: Register newest model as a shadow candidate
        run: python -m models.shadow --register-newest
      - name: Publish shadow candidate artifact
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f models/saved_models/model_v*.joblib models/saved_models/model_v*.json
          if git diff --cached --quiet; then
            echo "No candidate artifact changes"
          else
            git commit -m "chore(model): register shadow candidate"
            git push
          fi
      - name: Promote proven shadow candidate
        if: github.event_name == 'workflow_dispatch' && inputs.promote_candidate == true
        run: |
          test -n "${{ inputs.candidate_version }}"
          python -m models.shadow --promote "${{ inputs.candidate_version }}"
      - name: Refresh upcoming predictions
        if: github.event_name == 'workflow_dispatch' && inputs.promote_candidate == true
        run: python -m models.predict --model models/saved_models/latest.joblib --days 3
      - name: Publish promoted model artifact
        if: github.event_name == 'workflow_dispatch' && inputs.promote_candidate == true
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f models/saved_models/latest.joblib
          if git diff --cached --quiet; then
            echo "No model artifact changes"
          else
            git commit -m "chore(model): promote proven shadow candidate"
            git push
          fi
